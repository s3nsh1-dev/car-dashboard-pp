import React from "react";
import { AppBar, Toolbar } from "@mui/material";

const NavigationBar = () => {
  return (
    <AppBar>
      <Toolbar></Toolbar>
    </AppBar>
  );
};

export default NavigationBar;

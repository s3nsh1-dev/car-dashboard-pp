/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./index.html", // Adjust if `index.html` is in another folder
    "./src/**/*.{js,ts,jsx,tsx}", // Includes all your component files
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
